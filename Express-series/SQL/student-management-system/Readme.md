I'm trying to implement one to one association between two tables . (sequelize)

** many to many **
so we need a junction table where two tables primary key stored, the reference for many to many association
or
In a Many-to-Many relationship, both tables can have multiple records related to each other, so we use a junction table (also called a join table)